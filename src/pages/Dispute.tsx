import React from "react";
import { Box, Text } from "@chakra-ui/react";
import { useNavigate } from "react-router-dom";
import theme from "../theme";

const Dispute = () => {
  const navigate = useNavigate();
  return <Text>Dispute</Text>;
};
export default Dispute;
